{
	"name": "cashcompass",
	"version": "0.0.3",
	"private": true,
	"scripts": {
		"test:server": "rm -f /tmp/cashcompass_test.db && playwright test --config playwright.server.config.ts --reporter=list"
	},
	"devDependencies": {
		"@playwright/test": "^1.51.1"
	},
	"type": "module"
}
